# OS
.DS_Store

# 秘密情報・環境変数 (実体は gitignore 済みの .env に置く)
.env
.env.*
!.env.example

# エディタ・一時ファイル
*.swp
*.tmp

# ログ
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ビルドの入力 (データリポジトリの取得先) と出力。どちらもコミットしない
# (ADR 0015: 生成物は Pages のアーティファクトで配り、履歴にはソースだけ残す)。
# 末尾に / を付けない — 手元では別の場所へ symlink を張ることがある。
data-repos
